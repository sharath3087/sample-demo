name: DemoWorkflow2
on:
  push:
    branches: [main]
  workflow_dispatch:  
jobs:
  Demojob:
    permissions: write-all
    runs-on: ubuntu-latest
    steps:
      - name: First Step 1
        run: echo "Hi there..."
      - name: Second Step 2
        run: echo "${{ github.ref }}"
      - run: 'echo "$AZURE_UCAASACS_CONNECTION_STRING" > key'
        shell: bash
        env:
          AZURE_UCAASACS_CONNECTION_STRING: ${{secrets.AZURE_UCAASACS_CONNECTION_STRING}}
